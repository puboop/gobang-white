#include "piece.h"

int boardPoint[call_count][call_count]; // 棋盘坐标数组
int piece_color = 0; // 0 为白色，1为黑色

int main() {
	printf("输出>%d\n", 35 / 30);
	//initBoard(boardPoint); // 初始化棋盘
	//ExMessage mouse;// 声明鼠标消息结构体
	//drawPiece(piece_color, 450, 450);
	//while (1) {
	//	getmessage(&mouse, EX_MOUSE); // 获取鼠标消息
	//	if (mouse.message == WM_LBUTTONDOWN) {// 是否是左击消息
	//		piece_color = leftClick(piece_color, mouse, boardPoint);
	//	}
	//}
	return 0;
}